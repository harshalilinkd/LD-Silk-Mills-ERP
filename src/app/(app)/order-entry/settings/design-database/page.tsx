import { IconDatabase } from "@tabler/icons-react";
import { ComingSoon } from "@/components/shell/coming-soon";

export default function DesignDatabasePage() {
  return (
    <ComingSoon
      icon={IconDatabase}
      title="Design Database — coming soon"
      description="Browse and manage the log of every fabric + design used across orders."
    />
  );
}
